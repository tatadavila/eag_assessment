import dataSlice, { fetchPokemonsWithData, setPokemons } from "./dataSlices";
import uiSlice, {
  setLoading,
  setOpenCreatePokemon,
  setShowSearchInput,
} from "./uiSlice";

export {
  dataSlice,
  fetchPokemonsWithData,
  setLoading,
  setOpenCreatePokemon,
  setPokemons,
  setShowSearchInput,
  uiSlice,
};
